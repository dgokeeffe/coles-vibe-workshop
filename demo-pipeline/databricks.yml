bundle:
  name: grocery-intelligence-demo

variables:
  team_name:
    default: demo
  schema:
    default: demo

artifacts:
  grocery_intelligence:
    type: whl
    path: .

include:
  - resources/*.yml

targets:
  dev:
    default: true
    variables:
      team_name: demo
      schema: demo
